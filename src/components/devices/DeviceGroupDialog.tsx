import { Dialog, DialogContent, DialogHeader, DialogTitle } from "@/components/ui/dialog";

export const DeviceGroupDialog = () => {
  return (
    <Dialog>
      <DialogContent>
        <DialogHeader>
          <DialogTitle>Device Group</DialogTitle>
        </DialogHeader>
      </DialogContent>
    </Dialog>
  );
};

export default DeviceGroupDialog;