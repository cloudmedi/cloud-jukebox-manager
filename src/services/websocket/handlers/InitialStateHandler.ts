export const handleInitialStateMessage = (message: any) => {
  console.log('Initial state message received:', message);
  // Initial state mesajları React Query ile yönetiliyor
  // Bu handler şu an sadece loglama yapıyor
};