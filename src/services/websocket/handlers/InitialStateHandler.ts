export const handleInitialStateMessage = (message: any) => {
  console.log('Initial state received:', message);
  // Store'a initial state'i kaydet
  // Bu kısmı store yapınıza göre implemente edebilirsiniz
};